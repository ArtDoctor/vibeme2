/** Must match `FirstPersonControls` / `server/src/world.rs` (`EYE_HEIGHT`). */
export const EYE_HEIGHT = 1.65;

/** Horizontal capsule radius in XZ (circle-vs-AABB). Matches `server/src/world.rs`. */
export const PLAYER_RADIUS = 0.35;
